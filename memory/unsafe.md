# Unsafe

<!-- STUB -->

::: warning Not written yet
This page is an outline. The feature it describes works; the documentation for it does not.
:::

Planned sections:

- What `unsafe` actually unlocks, which is narrower than it sounds
- The promise you are signing: what the compiler stops checking
- Raw storage, and why `mem::init` exists beside an ordinary assignment
- Aliasing, and what two borrows of one object promise each other
- When you genuinely need it, using a container implementation as the example
- How to avoid needing it, which is usually the better answer
