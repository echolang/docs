# Copying

<!-- STUB -->

::: warning Not written yet
This page is an outline. The feature it describes works; the documentation for it does not.
:::

Planned sections:

- When a copy happens: assignment, an argument by value, a return
- The one copy the compiler writes for you, and the things that stop it
- Writing a copy constructor, and the shape the compiler recognises
- Why a copy constructor's source is `const` where it can be
- Deep copy versus a shared handle, and which one a class gives you
- `clone()` in the standard library, and why it is a method rather than a rule
- `#[unique]`, for a type only one value may ever name
