# Attributes

<!-- STUB -->

::: warning Not written yet
This page is an outline. The feature it describes works; the documentation for it does not.
:::

Planned sections:

- The `#[name: value]` shape, and the value grammar behind it
- Declaration attributes: `#[inline]`, `#[implicit]`, `#[unique]`
- Attributes the standard library uses and you should not: `#[core:]`, `#[builtin:]`, `#[intrinsic:]`
- Manifest attributes: `module`, `version`, `sources`, `depends`, `link`, `cc`, `build_dir`
- Conditional directives, which are their own thing: `#[if:]`, `#[elif:]`, `#[else]`, `#[end]`
- What happens to an attribute the compiler does not know
