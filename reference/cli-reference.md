# CLI reference

<!-- STUB -->

::: warning Not written yet
This page is an outline. The feature it describes works; the documentation for it does not.
:::

Planned sections:

- Usage, and the three subcommands
- Every option as a table: spelling, which subcommands accept it, what it does, what it defaults to
- What is built: `-o`, `-m`, `--build-dir`, `--link`
- How it is built: `--debug`, `--release`, `--optimize`, `-g`, `--track-allocations`, `--no-stdlib`
- What it is built for: `--target-os`, `--target-arch`, `--target-cpu`, `--target-features`, `--define`
- What it tells you: `-p`, `--explain`, `--diagnostics`, `--color`, `--silent`
- `clean` and its two options
- Retired spellings, and the message you get if you use one
- Environment variables that are honoured
