# Primitive types

<!-- STUB -->

::: warning Not written yet
This page is an outline. The feature it describes works; the documentation for it does not.
:::

Planned sections:

- The table: every primitive with its width, signedness, and range
- `usize` and `isize`, and what decides their width
- The aliases: `int`, `uint`, `float`
- `bool` and `void`
- What an untyped literal defaults to
- Literal forms: decimal, hex, the `f` suffix, and what does not exist
- Which conversions are implicit, which warn, and which are refused
