# Debugging

<!-- STUB -->

::: warning Not written yet
This page is an outline. The feature it describes works; the documentation for it does not.
:::

Planned sections:

- `echoc build -g` and what it adds
- Stepping through `.eco` source in lldb
- Why names have no `$` in the debugger
- `-g` versus `--debug`: two different axes
- `-g` and the optimizer, and why it turns optimization off unless you say otherwise
- Enabling the standard library's pretty-printers so an `array<T>` shows its elements
- What you will not see yet
- Why `-g` is build-only and `run` refuses it
- `dprint` as the printf-debugging option
