# std::env

<!-- STUB -->

::: warning Not written yet
This page is an outline. The feature it describes works; the documentation for it does not.
:::

Planned sections:

- Program arguments: `argc()`, `arg(i)`, `args()`, `argv()`
- Passing arguments through `echoc run` with `--`
- Environment variables: `has`, `var`, `var` with a fallback, `vars()`
- Directories: `working_dir()`, `home_dir()`, `tmp_dir()`
- `exe()` for the path to the running binary
- `pid()`
- `exit(code)`, and how it differs from `die`
- Why none of this needs a platform conditional in your code
