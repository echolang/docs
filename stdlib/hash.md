# hash

<!-- STUB -->

::: warning Not written yet
This page is an outline. The feature it describes works; the documentation for it does not.
:::

Planned sections:

- What a map needs from a key, and why it is not an interface
- The `hash::of` overload set, and what it already covers
- Teaching a map about your own type by adding an overload
- `hash::mix`, and why it is a finalizer rather than a byte walk
- `hash::bytes`, `hash::step`, `hash::combine`, `hash::of_bytes`
- Which bits of a hash the map actually uses, and why that decides how you write one
