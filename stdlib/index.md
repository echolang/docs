# The standard library

<!-- STUB -->

::: warning Not written yet
This page is an outline. The feature it describes works; the documentation for it does not.
:::

Planned sections:

- Three tiers, and how a name's spelling tells you which one it is in
- Tier one: the language's own vocabulary. `array`, `map`, `string`, `slice`, `range`, `die`, `assert`, `dprint`
- Tier two: still about the language, but not written every line. `contract::`, `mem::`, `str::`, `arr::`, `hash::`
- Tier three: ordinary utility. `std::math::`, `std::env::`
- What `#[core:]` means, and which types the compiler has to be able to name
- Building without it: what `--no-stdlib` takes away
- Where the source lives, and reading it as documentation
