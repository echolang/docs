# std::math

<!-- STUB -->

::: warning Not written yet
This page is an outline. The feature it describes works; the documentation for it does not.
:::

Planned sections:

- Constants: `PI`, `TAU`, `E`, `LN_2`, `SQRT_2` and friends, plus the `_F32` twins
- Why the constants have no `$`, and what that means at a use site
- The intrinsics: trigonometry, exponentials, logarithms, rounding, `sqrt`, `pow`, `fma`
- `min` and `max` across the numeric types
- `abs` and `clamp`
- Which of these lower to a single instruction
- The rough edges: a dead generic in `abs`, and a `clamp` with no integer widths
