# str and arr

<!-- STUB -->

::: warning Not written yet
This page is an outline. The feature it describes works; the documentation for it does not.
:::

Planned sections:

- `str::` is the string plumbing: `str::buf`, the refcounted byte block behind every `string`
- `str::from_c_str` and `str::view_of_c_str` for the C boundary
- `arr::merge` and `arr::with_capacity`
- Why these are small, and what a fuller string library needs first
